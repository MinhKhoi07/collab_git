// Lấy nút bấm bằng id
const button = document.getElementById("actionButton");

// Thêm sự kiện click cho nút
button.addEventListener("click", function() {
    // Hiển thị tên 3 thành viên nhóm
    alert("Thành viên nhóm: Tom, Jerry, Spike");
});